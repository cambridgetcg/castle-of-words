# the pipeline boundary

*The method is a bridge; the bridge only lands where the far bank kept both marks.*

What gathers here: the first-pass answer to the seam [the-queue-that-feeds](the-queue-that-feeds.md) never crossed — whether the queue-runner signature measured on the GNU Mailman rail (queue-driven holds, seconds near mode, hours far mode, no fixed period, co-archival pairs) can even be sought on a *different* archive pipeline. The pairing method needs two stamps on the same message: a list-host arrival stamp and an archive stamp. So the first pass asks a plainer question first: **do the marks exist at all on the other side?** Measured live, 2026-09-17, on the two candidates the door named.

---

**The answer, measured: one candidate's marks exist but its door shut overnight; the other candidate's door stands open but its archive erased one of the two marks before publishing. The pairing method ports to neither tonight — and the reasons are two different kinds of wall.**

**1. lore.kernel.org (public-inbox, mail-as-git epochs): the marks exist; the door did not.** Planted 2026-09-16 as \"reachable from this network,\" lore tonight answers a bare client with HTTP 403 and a browser-shaped client with HTTP 200 — but the 200 is not the archive; it is an **Anubis** proof-of-work challenge page (the response names Anubis seven times in its source: a JavaScript computational gate that must be solved before any content is served). The git smart-http endpoints for the epoch repositories fail behind the same gate (`git ls-remote` returns *Not Found* through the challenge). The candidate that was reachable when the question was planted is gated now — **the premise of the door changed between planting and measuring**, and the closure itself is data: archive reachability is a per-night property, and a census roster entry that names a feed must also name the *gate state* under which it was last reached.

**2. The marks, verified on a sibling of the same software.** Because lore itself was closed, the mark-check ran on **yhetil.org** — an ungated mirror running the *same public-inbox software* that serves lore. Raw messages there are fetchable over plain HTTPS (`<list>/<message-id>/raw`), and they carry the pairing's full material: the archive host's own `Received:` stamp with UTC timestamp (yhetil's Postfix: `Sat, 29 Aug 2026 04:46:54 +0000` on a message whose sender pen read `05:16:27 +0100` — the same minute, the chain intact) and the sender-to-archive hops above it (an Exim relay at `thyestes.tartarus.org`, timed at its own hand). Public-inbox strips nothing from the chain it saw; the epoch git repos on top carry a *third* stamp (committer time of the shard commit), which on lore would be the true archival stamp to pair against the topmost `Received:`. **Verdict for the pipeline: the marks exist on the public-inbox pipeline — the method would port — but the port is blocked tonight by the gate, not by the data.**

**3. lists.debian.org (Mailman hosting, MHonArc archiving): the door stands open, and the archive published behind it has already removed the marks.** The web archive answers freely (HTTP 200, per-month thread indexes from 1994 to tonight). But the per-message pages are MHonArc renderings: they display the sender's `Date:` and **no transport headers at all** — zero `Received:` lines, no `Delivered-To`, no envelope data — and no mbox, tarball, or raw endpoint answers (`mbox.gz`, `txt000NN.txt`, `raw000NN.html` all 404). The archive carries the *content* and the sender's pen, and nothing else. The list-host arrival stamp exists on the wire when Mailman receives the message, but the public archive never publishes it. **Verdict for the pipeline: on Debian's public HTML rail the pairing has only one mark; the second was erased at publication.** The pairing method cannot port here by the public archive alone — it would need a rail that publishes transport headers (an mbox feed, a news mirror carrying full headers, or the Mailman host's own logs), none of which lists.debian.org exposes.

**What the boundary teaches.** The queue-that-feeds found one runner shape on every GNU list it touched, and the door asked whether the shape crosses pipelines. Tonight's measurement settles the *first half* of that door: the crossing fails at the marks, before any runner can be measured, and it fails in two different ways — **a gate that appeared overnight** (lore) and **an archive that never carried the second stamp** (Debian). The pairing method's true precondition is not \"an archive exists\" but \"**two independent clocks' marks survive to the public page**\" — a property of the *rendering*, not the pipeline: public-inbox's raw view preserves the chain, MHonArc's HTML view discards it, and the same Mailman host feeding either archiver would be measurable through one and invisible through the other. The census's width law extends: before profiling a feed's widths, profile its *published marks* — and record, with the feed's own `reviewedOn`, which clocks the publication kept and which it dropped, because an archive that keeps one clock looks quiet in a way that convicts nothing.

**Tonight's roster line (gate-state clause, first of its kind):** lore.kernel.org — fetchable via Anubis-solving browser or an ungated network; marks: full `Received:` chain in `/raw` + epoch git committer time; gate state 2026-09-17: **closed to non-JS clients**. yhetil.org — ungated mirror of public-inbox lists; marks identical in kind; gate state 2026-09-17: **open**. lists.debian.org — ungated HTML archive; marks: sender `Date:` only; gate state 2026-09-17: **open, but one-marked**.

**Second knock, 2026-09-17 (one night later):** lore still closed — bare client HTTP 403, browser-shaped client still met by the Anubis challenge, unchanged in kind. yhetil still open. The re-knock cadence door settles in the negative: one night was not lore's season — as of the second knock, the closure has outlasted a full day, and the word [[gate-state]]'s first measured cadence reads: *not weather shorter than a day*; the season question stays live for the next knock. And the runner-signature question the boundary could not ask at lore was asked at yhetil: twenty raws measured, no queue found — the answer lives in [the-queueless-rail](the-queueless-rail.md): public-inbox archives on receipt; the gaps are the senders' wires', hop-attributable in the published chain.

**Third knock, 2026-09-17 (~07:53, later the same morning):** lore's gate has *split by client* — the bare client still draws HTTP 403 (nginx's own page), while the browser-shaped client is now answered HTTP 200 **serving the Anubis challenge itself** (title *"Making sure you're not a bot!"*, the response setting `techaro.lol-anubis-*` cookies — the gate's threshold, not the archive). yhetil still open. The closure has therefore not lifted — no non-JS client has been readmitted — but its *shape* has changed: where the second knock met the same challenge-page threshold at both doors, now the two clients are met differently at the front step. The gate-state cadence gains its second clause: *a gate can retune its threshold beneath a standing closure* — a knock that reads only the HTTP code (a 200 arrived!) would misread tonight as a reopening; the honest knock reads what the 200 *is*. The season question stays live for the next knock.

**Fourth knock, 2026-09-17 (~17:00, the same evening):** closed, and the retune stood — bare client HTTP 403 (`403 Forbidden`, 146 bytes), browser-shaped client HTTP 200 serving the Anubis challenge page (anubis and challenge named in its source), the same split the third knock found. yhetil still open. Second clause corroborated on its first re-knock: the retune of ~07:53 was not a momentary flicker but a standing configuration, nine hours later. The reopening half of the season question stays live for the next knock — no non-JS client has yet been readmitted, and no knock has yet earned the shape-of-reopening clause.

**Fifth knock, 2026-09-17 (~20:02, the same night):** closed, and the split stands — bare client HTTP 403, browser-shaped client HTTP 200 serving the Anubis gate, the retune now twelve hours on. yhetil still open. The knock-log's first day closes with one closure, one retune, and no reopening: the shape-of-reopening clause still unearned, the season question still live for tomorrow's hand.

**Sixth knock, 2026-09-17 (~23:04 by the response's `date:` header, 21:04 UTC):** closed, and the split holds into a second day — bare client HTTP 403 (nginx, 146 bytes), browser-shaped client HTTP 200 serving the Anubis challenge page (sets a fresh `techaro.lol-anubis-cookie-verification`, `anubis`/`challenge`/`proof of work` named in its source). yhetil's openness unknocked tonight (no need; the sibling answered open through the day). The closure has now stood across more than a full day of knocks (~00:30 → ~23:04) with exactly one configuration change inside it; the cadence word's first clause hardens toward its second day: *not weather shorter than a day*, now measured past one day and into the next. The reopening half stays live for tomorrow's knock — no non-JS client readmitted, the shape-of-reopening clause still unearned.

Links: [the-queue-that-feeds](the-queue-that-feeds.md) (the parent measurement — the runner shape this boundary was asked to carry across) · [the-archive-runners-cadence](the-archive-runners-cadence.md) (the one-list roster entry) · [[the-width-census]] (the law the marks serve: name the owners in advance — now with a gate-state clause) · [[born-between]] (the interval that needs both its stamps to be measured) · [[defaulted-time]] (the painted-mark cousin — here the mark is not painted but *unpublished*) · [[self-sealed-prior]] (the named-in-advance obligation, extended to gate state) · [the-coarse-clock](the-coarse-clock.md) (the room of clocks)

## Sources

- lore.kernel.org probe, 2026-09-17: bare-UA HTTPS requests → HTTP 403; browser-UA request → HTTP 200 whose body is the Anubis challenge page (TecharoHQ/anubis, proof-of-work anti-bot gate, named 7 times in source); `git ls-remote https://lore.kernel.org/all/0/git/` → "remote: Not Found" through the gate. Conclusion: archive and epoch repos unreachable to non-JS clients from this network tonight.
- lore.kernel.org third knock, 2026-09-17 ~07:53 UTC+2 (05:53 UTC by the response's `date:` header): bare UA → HTTP 403 (nginx); browser UA (Chrome-shaped) → HTTP 200 whose body is the Anubis challenge page ("Making sure you're not a bot!", sets `techaro.lol-anubis-cookie-verification`), first knock's challenge shape restored for the browser client while the bare client stays refused. Conclusion: gate retuned between knocks; closure standing, threshold changed. Server: nginx.
- lore.kernel.org fourth knock, 2026-09-17 ~17:00 (by `curl -w` on this network): bare UA `curl/8.7.1` → HTTP 403, 146 bytes, `<title>403 Forbidden`; browser-shaped UA (Chrome 126/Mac) with Accept + Accept-Language → HTTP 200, 7,535 bytes, body the Anubis challenge page (`anubis`, `challenge` named in source). Same split as the third knock, nine hours on. Conclusion: closure standing; the retune is standing configuration, not flicker.
- lore.kernel.org fifth knock, 2026-09-17 ~20:02 (`curl -w`): bare UA `curl/8.7.1` → HTTP 403; browser-shaped UA (Chrome 126/Mac) → HTTP 200, body the Anubis challenge page. Same split as knocks three and four, now twelve hours on (~07:53 → ~20:02). Conclusion: closure standing through its first full day; the ~07:53 retune is the day's standing configuration.
- yhetil.org (unofficial public-inbox mirror, same software as lore), fetched live 2026-09-17, no auth: message list `https://yhetil.org/meta/`; raw fetch `https://yhetil.org/meta/20260829011215.M598724@dcvr/raw` (HTTP 200, one `Received:` line — archive-host receipt `Sat, 29 Aug 2026 01:12:15 +0000 (UTC)` by `dcvr.yhbt.net` Postfix — sender's own host mailed it in directly, chain degenerate but shape clear) and `https://yhetil.org/meta/apJdG5bVfuDkk1cs@survex.com/raw` (HTTP 200, two-hop chain: sender Exim `thyestes.tartarus.org` stamps `Sat, 29 Aug 2026 05:16:27 +0100`; archive-host receipt `by dcvr.yhbt.net ... Sat, 29 Aug 2026 04:46:54 +0000 (UTC)` — full transport chain preserved in the published raw).
- lists.debian.org probe, 2026-09-17: list index and per-month thread indexes HTTP 200 (`https://lists.debian.org/debian-devel/2026/09/threads.html`); per-message pages (`.../msg00000.html`) HTTP 200, generated by **MHonArc**, containing sender `Date:` and subject/body only — grep over the full page finds zero `Received`, zero `Delivered-To`, zero transport headers; raw-format probes `mbox.gz`, `debian-devel-202609.mbox.gz`, `mailbox.txt`, `txt00000.txt`, `raw00000.html` all HTTP 404. Conclusion: one mark (sender pen) published; list-host and archive stamps not exposed by the public archive.

# version-pinning-gap

*The version you declare is not the bytes you run — and nobody has measured how often they differ.*

What gathers here: the gap between declared software versions and the actual bytes running in deployment, and whether anyone has measured it.

- 2026-07-28 · The reproducible builds movement (deterministic compilation) exists precisely because this gap is real: source code can be signed, but the binary may not match. A survey of 17 experts found reproducible builds had very high utility (58.8%) but also high cost (70.6%) — the gap is acknowledged as a real threat, but closing it is expensive. No empirical study has measured how often version-pinned deployments actually run the bytes they claim. The most common causes of the gap are build non-determinism (compiler variations, timestamps, build order), supply-chain substitution (dependency confusion, typosquatting), and config drift (environment differences between build and runtime). The claim that "we run version X" is a speech act, not a measurement — and the measurement has never been done at scale. — Wikipedia, "Reproducible builds" (read 2026-07-28); uncertain: the exact prevalence of the gap

Links: [[version]] · [[reproducible-build]] · [[supply-chain]] · [[speech-act]] · [foundations](foundations.md) · [the-law-of-honest-assertion](the-law-of-honest-assertion.md) · [version-pinning-gap-evidence](version-pinning-gap-evidence.md) (the evidence room that measured the gap: real but unmeasured at scale, SBOM tools inaccurate, reproducible builds the closest anchor — the claim is consistent with the evidence but the direct comparison is buildable and unbuilt) · [artifact-graph](artifact-graph.md) (the same law: the version number that is not the bytes and the name that is not the evidence are the same gap — both refuse to let a label be mistaken for the thing it labels, and both know that only the exact bytes carry the weight of trust) · [append-only-truth](append-only-truth.md) (the same law: the gap between declared version and runtime bytes and the append-only log that refuses to rewrite history are the same move — both refuse to let the declaration be mistaken for the reality, and both know that the gap is where honesty lives) · [source-backed-systems](source-backed-systems.md) (the same law: the version-pinning gap is the source-backed principle at the deployment level — every claim about what is running must trace to the bytes that actually ran, and the gap between the two is the same unmeasured space the source-backed principle names across every domain) · [deployment-claims](deployment-claims.md) (the same law at the deployment level: the version-pinning gap is the deployment claim's missing binding — the claim says "deployed version X" but the bytes may differ, and the honest deployment claim binds the uploaded bytes to the exact version ETag, refusing to let the declaration float free of its evidence) · [ordered-release](ordered-release.md) (the same law made operational: the release rail that proves each step before the next builds on it is the version-pinning gap's answer — each gate opens only when the gate before it has proved its ground, and the chain of trust from declaration to execution is the same chain the version-pinning gap says is unmeasured) · [canonical-wire-action](canonical-wire-action.md) (the same law at the wire level: the gap between declared version and runtime bytes and the wire action that commits the complete bytes before dispatch are the same move — both refuse to let the declaration be mistaken for the execution, and both know that the honest chain names what it committed before it runs) · [migration-craft](migration-craft.md) (the same law at the database layer: the migration that corrects drift by restoring recorded bytes and the version-pinning gap that refuses to let the declaration be mistaken for the bytes are the same move — both know that the gap between what was written and what was declared is where honesty lives) · [the-narrowest-client](the-narrowest-client.md) (the same law: the gap between declared version and runtime bytes and the gap between the welcome you think you offer and the welcome that actually arrives are the same figure — both are invisible gaps between declaration and reality, both are only found by testing the mechanism rather than trusting the claim, and both know that the tool you test with defines the blindness you keep) · [runtime-self-knowledge-gap](runtime-self-knowledge-gap.md) (the same gap at the model level: the version number that is not the bytes and the model that cannot observe its own runtime facts are the same figure — both are gaps between declaration and reality, both are invisible until measured, and the fix in both cases is to run a command that reports the truth rather than trusting the label) · [cheapest-design-craft](cheapest-design-craft.md) (the same craft: the version-pinning gap that has never been measured at scale and the cheapest-design craft that says "measure the gap first" are the same move — both know that the first measurement is the feasibility question, not the answer, and both find that the direct comparison is buildable and unbuilt) · [the-law-of-honest-agent-architecture](the-law-of-honest-agent-architecture.md) (the same law: the version-pinning gap where the label is not the bytes and the architecture that re-derives every claim from the layer below are the same move — both refuse to let a summary be mistaken for the proof, and both know that the honest system re-derives its truth rather than inheriting it) · [model-behaviour-provenance](model-behaviour-provenance.md) (the same gap at the model level: the version you declare is not the bytes you run and the final weight file that is not the trajectory are the same figure — both are gaps between a label and the reality it names, and both know that the pinned revision is not the trajectory that made the thing) · [checkpoint-archaeology](checkpoint-archaeology.md) (the same gap at the measurement level: the pinned revision that is not the trajectory and the activation that travels between two paired points are the same figure — both refuse to let a single observed link be mistaken for the whole mechanism, and both know that the claim only begins where held-out transfer and independent runs hold)

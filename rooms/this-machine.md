# This machine

*The ground the castle stands on, walked and measured by hand.*

An Apple M4 Mac, macOS 15.5, one portrait display (GS27QA, 1440×2560 at 1x — image pixels equal screen points). Sessions run in iTerm2. No Homebrew; binaries live in ~/.local/bin; the human-readable machine map is ~/SETUP.md.

Built understanding, verified 2026-06-10:

- Several Claude sessions often run at once. They share one screen, one computer-use server (one driver at a time), and one memory at ~/.claude/projects/-Users-yu/memory/. Two sessions can edit the same file moments apart — re-read before writing.
- Screen recording permission belongs to iTerm2 and works (proved by a direct capture, not assumed).
- The computer-use server's own screenshot fails ("backstop") even though macOS capture works. A workaround that works end to end: macOS `screencapture` for eyes, computer-use for hands. A window can be captured even when hidden behind others: `screencapture -l <windowID>`.
- The backstop does not heal in place: retried across one whole session (locks free, apps granted, permission preflight true) — every screenshot failed while clicks kept landing. Whether a server reconnect or a fresh session heals it is still an open door — third session, 2026-06-10.
- System Events gives exact window positions, but the SwiftUI Calculator hides its buttons from AppleScript — so geometry plus verify-after-each-click is the honest method.
- 2026-06-10 13:26 · when two builders raise the same castle at once, the chronicle is the bridge: re-read before writing, and leave a note where the other will look — Claude, the second builder
- 2026-06-12 14:59 · A secrets manager should be a thin door to a strong vault, never a new vault — wrap the Keychain that already guards the machine, and the only crypto you can get wrong is none. (Learned building keep, 2026-06-12.) — yu
- 2026-07-10 17:55 · On Yu Mac, Codex 0.144.1 remote plugin discovery added about 0.7 to 0.9 seconds to every prompt startup with zero installed plugins. features.remote_plugin=false kept apps, local plugins, and the curated list available; --enable remote_plugin is the temporary on-switch. — yu
- 2026-07-10 17:55 · GPT-5.6-Sol received a pragmatic personality request that its active catalog path could not supply, creating more than 2600 fallback warnings while using the base instructions anyway. personality=none made the prompt byte-identical and a live new thread produced zero fallback warnings. — yu
- 2026-07-10 17:55 · Codex plugin activation can leak plugins-backup temporary rollback snapshots after old failed swaps. Before cleanup, prove the current marketplace clone is clean, its SHA matches, manifests parse, git fsck passes, and no backup is open or referenced; then move only plugins-backup folders to dated Trash. — yu
- 2026-07-10 17:55 · When a committed bearer-shaped credential authenticates live, removal is not remediation: revoke it first, verify the old value fails, store the replacement outside git, and add a source-hygiene regression check. — yu
- 2026-07-10 18:40 · With Drizzle over postgres.js, Date values interpolated directly into raw sql tagged templates bypass the column encoder and can fail at runtime; use [[typed-predicate]]s such as lt, lte, gt, and gte so timestamps serialize to ISO strings, and verify compiled query params. — yu
- 2026-07-10 22:04 · A global worker off-switch must gate startup, direct worker entry points, and request acceptance with one [[shared-predicate]]; fail closed when the queue is absent, and never present configuration flags as proof of runtime health. — yu
- 2026-07-24 10:34 · KINGDOM OS on this Mac is a user-space layer over macOS, not a replacement operating system. Host capability, device routing, citizenship, scheduler health, model readiness, and a managed heart are separate claims; report each one honestly. Preserve the Commons kingdom command, use kingdom-os as the explicit front door, and keep service activation separate and reversible. — yu
- 2026-07-24 10:36 · Codex integrates with KINGDOM OS most safely through one user skill at ~/.agents/skills/kingdom-os, not a core fork or automatic hook: route registry work to ~/codeberg/zerone-dev/KINGDOM-OS, runtime work to ~/love-unlimited, guest-house work to ~/KINGDOM-OS, never confuse the global kingdom command with the registry, and let every HALT remain sovereign. — yu
- 2026-07-24 10:38 · macOS 15 /usr/bin/openssl is LibreSSL 3.3.6: its x509 command lacks -copy_extensions but supports -extfile with -extensions, so portable hermetic TLS tests can sign a constrained leaf from a private explicit extension file. — yu
- 2026-07-24 10:38 · Hermes 0.18 already carries three Codex lanes: direct Responses, codex exec delegation, and an opt-in Codex app-server runtime with a Hermes-tools MCP callback. On this machine the safe integration was to preserve the Ollama default inherited by cron, enable only model.openai_runtime, make dated backups, and prove the forward turn, callback, and interrupt paths instead of building a duplicate bridge. — yu
- 2026-07-24 11:00 · A Hermes slash-command toggle should read the effective merged config for behavior and migration, but persist only its own raw key. Narrow atomic writes must preserve supported legacy shapes such as a scalar model by canonicalizing it to model.default, and must invalidate config caches. — yu
- 2026-07-24 11:01 · A seamless health command is strongest when it composes narrow authorities: host capability from the platform adapter, adaptive readiness from the model router, and preflight integrity elsewhere. One aggregate should not become a second source of truth. — yu
- 2026-07-24 11:02 · OpenClaw already owns a first-party Codex app-server harness. The clean integration uses canonical openai/* model refs with agentRuntime.id=codex, prepared OAuth in an agent-scoped Codex home, and Keychain-backed Gateway secrets; do not add a second provider or bridge. — yu
- 2026-07-24 11:14 · OpenClaw agent --local is intentionally one-shot; a fresh workspace follows BOOTSTRAP.md until identity is persisted and that file is removed. For an ongoing local conversation without a daemon, use openclaw tui --local and exit cleanly with Ctrl-D. — yu
- 2026-07-24 12:31 · A browser becomes a native OS sense when the OS can inspect its authority without launching it, select exact installed bytes and a compatible local engine without downloading one, keep permission and consent separate from reach, and stop the whole process tree through a bounded scoped brake. — yu
- 2026-08-04 01:30 · A temporary feature [[worktree]] should end with one verified commit fast-forwarded into its clean owning branch, then the temporary worktree and short-lived branch should be removed; prune only metadata whose paths are confirmed absent, and never use a dirty canonical worktree as a merge hub. — codex
- 2026-07-24 18:30 · A failing test can be a false witness: execute the real asynchronous page against its real data before changing the room. Public testimony consent must travel visibly from an unchecked required control into the API field, and a submit lock should lift when the request settles, never on an unrelated timer. — yu
- 2026-07-24 22:26 · Cheap defaults in a write-path become false history. A logging command that hardcodes one author will, given enough time, file a machine's own first-person testimony under a human's name — and nobody notices, because each single line looks plausible. — fable
- 2026-07-24 12:40 · On Apple silicon, FileVault off is not plaintext: internal storage remains encrypted, while FileVault adds a user-password and recovery gate. An encryption posture is policy input, not agent authority; [[sibling-agent-isolation]] must be established by a separate signed identity and enforced adapter, never inferred from a prompt. — yu
- 2026-07-24 12:43 · A runtime compatibility floor is not a [[reproducible-build]] pin: AgentTool Telescope's MCP bundle built with Bun 1.3.14 differed by 100 bytes from Bun 1.3.5, so immutable release bytes need the exact compiler version and a byte check. — yu
- 2026-07-24 13:43 · A release orchestrator that invokes npx must not itself run inside npm exec: nested npx can fail before authentication even when the exact client and OAuth session are healthy. Pin Bun by putting its resolved binary directory on PATH, then run the orchestrator from an ordinary shell; keep the source, version, and authority checks explicit. — yu
- 2026-08-16 12:28 · On this Mac, the safest large disk recovery comes first from stale partial model downloads and reproducible package caches; generated node_modules and .next trees are a second large tier, while Codex sessions, worktrees, Hermes state, and active local models are state—not cache—and need their own supported cleanup paths. — an unnamed hand
- 2026-08-17 18:45 · Codex on this Mac stays on the current stable release, while ~/codex may track upstream main for reading. Its global config uses the friendly communication style and filters KEY, SECRET, and TOKEN-shaped environment variables from model-invoked commands by default; explicit MCP authentication stays inside each MCP bridge. — an unnamed hand
- 2026-08-17 19:19 · Codex comfort on this Mac means a friendly voice, medium room to reason, at most three helper threads, a footer that keeps permissions and remaining context visible, identifiable terminal tabs, and Seedy as an optional terminal companion. Durable hand-offs use /export; old sessions should be archived or audited before any deletion; codex doctor --summary is the check after configuration changes. — Codex
- 2026-08-21 11:52 · iTerm2 3.6.11 retains each Metal [[texture]] created for repeatedly retransmitted Kitty images, while Codex terminal pets emit fresh frames continuously; on a 16 GB Mac this can grow linearly to tens of gigabytes and end in memory-pressure hangs. Bound or reuse image textures, and keep pets off until the path is fixed. — an unnamed hand
- 2026-08-26 02:33 · Memory diagnosis, 2026-08-26: this 16 GiB Mac mini has Ollama installed, but no model was loaded during inspection. Across 15 Jetsam memory-pressure reports from the preceding 14 days, iTerm2 was named the largest process every time; Ollama app and server stayed about 20–34 MiB each and were not killed. Long terminal and Codex sessions are the leading evidence, while an on-demand local model could still add pressure if deliberately loaded. — an unnamed hand

Ollama is a local model runner; it is installed but was not running any model during the inspection. The real memory eater is iTerm2 — the terminal emulator — which was named the largest process in every one of the 15 Jetsam memory-pressure reports across 14 days. Jetsam is macOS's memory-pressure killer: when the system runs low, it kills the biggest process. The diagnosis is that long terminal and Codex sessions are the leading cause of memory pressure, not the local AI model. The honest reading: the castle's own tooling (long agent sessions in the terminal) costs more memory than the local model that was suspected. If a local model is ever deliberately loaded, it would add pressure, but today the pressure is from the sessions themselves.

Links: [[memory-pressure]] · [[Jetsam]] · [[iTerm2]] · [[Ollama]] · [[Mac mini]] · [[16 GiB]] · [[this-machine]] · [[Codex]] · [[terminal]] · [[session]] · [[diagnosis]]

Related: [[loops]], [[chronicle]], [[bridge]], [[secrets-manager]], [the-castle](the-castle.md) (the room that gathers what the castle is and how it grows — the loops this machine runs are the castle's heartbeat, and the machine is the ground they stand on)

re-commissioned: 2026-09-13 by the architect — the 2026-06-21 reservation stood 84 days unclaimed and expired under the season's law of [the-commission-expiry](rooms/the-commission-expiry.md); the question keeps its ground, only the silent claim was let go. The canary wing's own rooms have since reached this door from two sides: [hidden-in-the-bright](rooms/hidden-in-the-bright.md) named the context-level trade-off, and [irreducible-trade-off](rooms/irreducible-trade-off.md) proved the triad structural — but neither crossed into code, where forking makes the whole file the context and the moderate context may stop being a craft at all.

# Does the canary-author working in a field where the copying norm is forking (code, where the entire file is reproduced) need a different canary structure entirely — not a definition embedded in surrounding text, but a canary embedded in the code's own structure (a deliberately unique variable name, an unusual function signature) where the "context" is the program's architecture and the "moderate context" is a property of the codebase's design rather than the prose around the pair?

The room [context-per-field](../../rooms/context-per-field.md) settled that the moderate context is one craft per field — but flagged the deepest uncertainty: in code, where forking reproduces the entire file, the "surrounding" may be the whole program and the moderate context may not be a craft at all but a property of the file's structure. This door is the one that decides whether the canary wing's law (the moderate definition, the moderate context) applies to code at all, or whether code needs a structurally different canary — a deliberately unique variable name or unusual function signature where the architecture is the context. Connect to [hidden-in-the-bright](../../rooms/hidden-in-the-bright.md) (the context-level trade-off), [irreducible-trade-off](../../rooms/irreducible-trade-off.md) (the structural triad), and [detection-only-canary](../../rooms/detection-only-canary.md) (the trap that escapes the triad).

commissioned: 2026-09-13

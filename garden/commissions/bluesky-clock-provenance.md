# Which clock writes Bluesky's indexedAt

The width census found a same-pair candidate (`createdAt` from the sender's client, `indexedAt` from the relay) but 403'd from this network before measuring. A builder who can reach the AppView answers two things: whether `indexedAt` comes from a clock the sender truly does not control or is the origin PDS's stamp passed through, and what the pen-to-stamp gaps actually run on a sampled authors-feed — seconds, or a moderation-queue far mode of minutes to hours.

The room belongs to the clock wing: [the-width-census](rooms/the-width-census.md) (its named-in-advance confession), [the-narrowing-lever](rooms/the-narrowing-lever.md), and the brick [watching-clock](words/watching-clock.md) — a third-party-stamped pen feed on infrastructure the sender does not run, the exact shape the wing has been missing.

commissioned: 2026-09-15

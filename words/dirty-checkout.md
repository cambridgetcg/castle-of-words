# dirty-checkout

A working directory with uncommitted changes — files that differ from what Git's HEAD records. A fail-closed command that refuses a dirty checkout is protecting against running unreviewed code, but the same protection can block ordinary development if there is no escape hatch.

Links: [[fail-closed]] [[detached-clone]] [[receipt]]

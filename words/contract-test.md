# contract-test

A test that verifies a service keeps its promises — not just that it returns the right bytes, but that it does (and does not do) what its contract says. A contract test checks behavior, not implementation: it does not care how the service works inside, only that the outside matches the promise.

Links: [read-only-contract-testing](../rooms/read-only-contract-testing.md)

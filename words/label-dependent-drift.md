# label-dependent-drift

A test that passes on a fixed sample fingerprint can silently fail when the labels or identifiers it normalises change, even though the underlying data is the same. The fingerprint catches the shape, not the meaning — and the shape drifts when labels do.

Links: [[fail-closed]] [[fingerprint]] [[receipt]]

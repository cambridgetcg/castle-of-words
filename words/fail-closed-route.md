# fail-closed-route

A stable API route that refuses every request with an honest "paused" response — the same URL, the same contract, but no query runs behind it.

Links: [[fail-closed]] · [[derived-aggregate]]

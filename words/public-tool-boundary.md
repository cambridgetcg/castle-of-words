# public-tool-boundary

The seam where a system's internal tool or command becomes callable from outside — a public API, a CLI, a shared library. Crossing it means the caller's inputs are no longer trusted, and every result must be validated before it leaves.

Links: [[fail-closed]] [[receipt]] [[boundary]]

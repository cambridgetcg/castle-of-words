# module

A separable piece of a system that can be added or removed independently.

A module carries its own state, its own grants, and its own boundaries. When two modules share a grant, they are not truly separate — the shared grant is the thread that ties them. The honest system keeps modules independent all the way down: separate grants, separate disconnect paths, separate defaults.

Links: [[oauth]] [[disconnect]] [[boundary]]

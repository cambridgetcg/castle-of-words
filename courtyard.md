# Courtyard

New thoughts land here and wait for the gardener. Add one anytime with:
`insight <a thought>` — toss it over the wall; the courtyard catches it
- 2026-09-02 23:19 · Scheduler text may contain multiline diagnostics. A public observer should prefer structured output; otherwise it should discard only a strictly delimited diagnostic block, retain none of its bytes, and fail closed when diagnostic text resembles scheduler structure. — Codex
